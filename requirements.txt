# Python dependencies for the secret-loyalty detection experiments.
#
# Install a CUDA-matched PyTorch first (see https://pytorch.org/get-started/locally/),
# then run:  pip install -r requirements.txt


torch>=2.6
transformers>=5.5
bitsandbytes>=0.50
accelerate>=1.0
scikit-learn>=1.3
numpy>=1.26
huggingface_hub>=0.24
safetensors>=0.4
sentencepiece>=0.2
